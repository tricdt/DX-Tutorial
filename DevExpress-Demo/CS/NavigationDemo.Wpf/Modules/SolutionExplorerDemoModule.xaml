<local:AccordionDemoModule
    x:Class="NavigationDemo.SolutionExplorerDemoModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxdb="http://schemas.devexpress.com/winfx/2008/xaml/demobase/internal"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    xmlns:local="clr-namespace:NavigationDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800"
    DataContext="{dxmvvm:ViewModelSource local:SolutionExplorerViewModel}">

    <local:AccordionDemoModule.Resources>
        <local:SolutionNodeImageSelector x:Key="solutionNodeImageSelector" />
    </local:AccordionDemoModule.Resources>
    <DockPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="200" MaxWidth="950" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition MinWidth="200" />
            </Grid.ColumnDefinitions>
            <dxg:TreeViewControl
                x:Name="treeViewControl"
                ChildNodesPath="Children"
                ShowSearchPanel="True"
                ItemsSource="{Binding Nodes}"
                TreeDerivationMode="ChildNodesSelector"
                TreeViewFieldName="DisplayName"
                ShowNodeImages="True"
                NodeImageSelector="{StaticResource solutionNodeImageSelector}"
                ShowBorder="False"
                Loaded="{DXEvent Handler='@Self.ExpandNode(0);@Self.ExpandNode(1)'}">
                <dxmvvm:Interaction.Behaviors>
                    <local:SolutionExplorerBehavior CodeView="{Binding ElementName=codeView}" />
                </dxmvvm:Interaction.Behaviors>
            </dxg:TreeViewControl>

            <dxdb:CodeViewPresenter x:Name="codeView"  Grid.Column="2" AutoExpandAllRegions="True" BorderThickness="0"/>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Padding="4,0,4,0" />
        </Grid>
    </DockPanel>
</local:AccordionDemoModule>