<local:EditorsDemoModule x:Class="EditorsDemo.TimePickerModule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EditorsDemo"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <Grid Width="400"
              HorizontalAlignment="Center"
              VerticalAlignment="Top"
              Margin="0,60,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <dxe:TimePicker Margin="0,0,0,24"/>
            <StackPanel Margin="0,24,0,12" Grid.Row="1">
                <Label Content="DateEdit with Calendar and TimePicker" />
                <dxe:DateEdit x:Name="DateEditWithNavigatorAndTimePicker" Mask="G" MaskUseAsDisplayFormat="True">
                    <dxe:DateEdit.StyleSettings>
                        <dxe:DateEditNavigatorWithTimePickerStyleSettings/>
                    </dxe:DateEdit.StyleSettings>
                </dxe:DateEdit>
            </StackPanel>
            <StackPanel Margin="0,12" Grid.Row="2">
                <Label Content="DateEdit with TimePicker" />
                <dxe:DateEdit x:Name="DateEditWithTimePicker" Mask="T" MaskUseAsDisplayFormat="True">
                    <dxe:DateEdit.StyleSettings>
                        <dxe:DateEditTimePickerStyleSettings/>
                    </dxe:DateEdit.StyleSettings>
                </dxe:DateEdit>
            </StackPanel>
        </Grid>
    </DockPanel>
</local:EditorsDemoModule>